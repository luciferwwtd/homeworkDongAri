random - 확률과 관련된 기능을 가진 모듈이다.
사용법 : 모듈이므로 import random을 한 후 사용한다.

import random - random 모듈을 가져온다.
random.randint() - 두 수를 넣으면 그 두 수 사이에서 랜덤한 수를 하나 리턴한다.

[예시]

import random

a = random.randint(1, 10)
print(a)
